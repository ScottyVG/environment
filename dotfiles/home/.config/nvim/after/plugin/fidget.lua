-- Fidget.nvim setup for LSP progress indicators
-- Based on TJ DeVries' configuration

require("fidget").setup({
  -- Options for fidget can be configured here
  -- The default setup is usually sufficient
})